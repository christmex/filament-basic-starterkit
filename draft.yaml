models:
  SchoolYear:
    name: string unique
    status: boolean default:false

  SchoolTerm:
    name: string unique
    status: boolean default:false

  Student:
    name: string
    nis: string nullable
    nisn: string nullable 

  Classroom:
    name: string unique
  
  
