models:
  SchoolYear:
    name: string unique
    status: boolean

  SchoolTerm:
    name: string unique
    status: boolean

  Student:
    name: string
    nis: string nullable
    nisn: string nullable 

  Classroom:
    name: string unique
  
  
